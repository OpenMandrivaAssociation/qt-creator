--- qt-creator-opensource-src-20.0.1/src/plugins/clangformat/clangformatutils.cpp
+++ qt-creator-opensource-src-20.0.1/src/plugins/clangformat/clangformatutils.cpp
@@ -67,7 +67,9 @@ clang::format::FormatStyle calculateQtcS
     style.AllowShortBlocksOnASingleLine = FormatStyle::SBS_Never;
     style.AllowShortCaseLabelsOnASingleLine = false;
 #if LLVM_VERSION_MAJOR >= 23
-    style.AllowShortFunctionsOnASingleLine = {false, true, false};
+    // ShortFunctionStyle is a struct since LLVM 23 (was enum)
+    style.AllowShortFunctionsOnASingleLine =
+        FormatStyle::ShortFunctionStyle::setInlineOnly();
 #else
     style.AllowShortFunctionsOnASingleLine = FormatStyle::SFS_Inline;
 #endif
@@ -82,12 +84,14 @@ clang::format::FormatStyle calculateQtcS
     style.AlwaysBreakTemplateDeclarations = FormatStyle::BTDS_Yes;
 #endif
 #if LLVM_VERSION_MAJOR >= 23
-    style.PackArguments = { FormatStyle::BPAS_OnePerLine };
+    // BinPackArguments was replaced by PackArguments in LLVM 23
+    style.PackArguments.BinPack = FormatStyle::BPAS_OnePerLine;
 #else
     style.BinPackArguments = false;
 #endif
 #if LLVM_VERSION_MAJOR >= 23
-    style.PackParameters = { FormatStyle::BPPS_OnePerLine };
+    // BinPackParameters was replaced by PackParameters in LLVM 23
+    style.PackParameters.BinPack = FormatStyle::BPPS_OnePerLine;
 #elif LLVM_VERSION_MAJOR >= 20
     style.BinPackParameters = FormatStyle::BPPS_OnePerLine;
 #else
